# Examples

Milestone 1 examples are intentionally minimal. Run the CLI with a mocked or live read-only RPC endpoint:

```bash
eth-analyzer analyze 0x0000000000000000000000000000000000000000 --rpc-url http://localhost:8545 --output analysis
```
